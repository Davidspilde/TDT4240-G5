package io.github.Spyfall.stages;

import com.badlogic.gdx.utils.viewport.ScreenViewport;

public class CreateGameStage extends StageController {
    public CreateGameStage(ScreenViewport viewport) {
        super(viewport);
    }
}
